# Base URL of the SDAKW backend API.
# Must include the /api/v1 prefix when the frontend and backend are separate
# deployments, e.g. http://localhost:4000/api/v1
# Used by the centralized Axios instance (src/lib/api/client.ts).
# Leave empty for local development when no backend is running.
NEXT_PUBLIC_API_URL=